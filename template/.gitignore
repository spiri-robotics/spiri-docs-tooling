docs/_build/
.venv/
__pycache__/

# `vale sync` downloads style packages into styles/. The vocabulary under
# styles/config is ours and is committed; everything else is fetched.
styles/*
!styles/config/
